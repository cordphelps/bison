# boxplot generation

boxplot(percent.total.aesthetics ~ just.checking, data=bison.df, ylab="news article fraction", xlab="aesthetic keywords", las=1)

boxplot(percent.total.biodiversity ~ just.checking, data=bison.df, ylab="news article fraction", xlab="biodiversity keywords", las=1)